<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jnetdata.simple.manage.site.mapper.SiteMapper">
    <select id="getSiteTree" resultType="com.jnetdata.simple.manage.site.model.Site">
       select COMPANYID id,CPYNAME name,0 companyid from companyinfo
       UNION ALL
       select a.siteid id,a.sitename name,a.companyid companyid
       from websiteinfo a,companyinfo b where a.companyid = b.companyid and (a.status != 1 or a.status is null)
    </select>
</mapper>
